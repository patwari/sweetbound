export default class Player extends Phaser.Physics.Arcade.Sprite {
  constructor(scene, x, y, texture = null, frame = null) {
    super(scene, x, y, texture, frame);
    // Stub: texture is required for a visible sprite later.
  }
}
