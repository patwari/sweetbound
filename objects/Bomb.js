export default class Bomb extends Phaser.Physics.Arcade.Sprite {
  constructor(scene, x, y, texture = null, frame = null) {
    super(scene, x, y, texture, frame);
    // Stub: explosion behavior will be added later.
  }
}
