export default class LavaScene extends Phaser.Scene {
  constructor() {
    super({ key: "LavaScene" });
  }

  preload() {
    // No assets yet.
  }

  create() {
    // Lava level setup will go here.
  }
}
