export default class HUDScene extends Phaser.Scene {
  constructor() {
    super({ key: "HUDScene" });
  }

  preload() {
    // No assets yet.
  }

  create() {
    // HUD elements will be added here.
  }
}
