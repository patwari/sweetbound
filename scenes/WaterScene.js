export default class WaterScene extends Phaser.Scene {
  constructor() {
    super({ key: "WaterScene" });
  }

  preload() {
    // No assets yet.
  }

  create() {
    // Water level setup will go here.
  }
}
